version: 0.2

phases:
  install:
    commands:
      - echo "✅ INSTALL PHASE test 1"
      - npm install
  build:
    commands:
      - echo "✅ BUILD PHASE"
      - exit 1 # 👈 Intentionally fail the build
