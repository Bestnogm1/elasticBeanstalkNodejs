version: 0.2

phases:
  install:
    commands:
      - echo "✅ INSTALL PHASE"
      - npm install
  build:
    commands:
      - echo "✅ BUILD PHASE"
      # Optional: run tests or build steps here
      - zip -r app.zip . -x "*.git*" "node_modules/*"

artifacts:
  files:
    - app.zip
