version: 0.2

phases:
  install:
    commands:
      - echo "✅ INSTALL PHASE"
      - npm install

  build:
    commands:
      - echo "✅ BUILD PHASE"
      - zip -r output.zip . -x ".git/*" "README.md" "template.yaml" "test.txt"
      - echo "🧪 Contents of current dir:"
      - ls -la
      - echo "📦 Verifying output.zip:"
      - unzip -l output.zip

artifacts:
  files:
    - output.zip
  discard-paths: yes
  base-directory: .
